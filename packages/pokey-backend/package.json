{
  "name": "pokey-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:functional": "vitest run --config vitest.functional.config.ts",
    "test:all": "vitest run && vitest run --config vitest.functional.config.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1003.0",
    "@aws-sdk/lib-dynamodb": "^3.1003.0",
    "ajv": "^8.18.0",
    "express": "^5.2.1",
    "pokey-common": "*",
    "prom-client": "^15.1.0",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "testcontainers": "^11.12.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
